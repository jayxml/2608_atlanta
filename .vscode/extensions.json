{
  // When this repo is opened in VS Code or SAP Business Application Studio,
  // the editor offers to install these. The mermaid extension lets the built-in
  // Markdown preview render the diagrams in architecture_playbook.md and the
  // workshop notebooks. (Alternatively, open architecture_playbook.html — it
  // renders everything offline with no extensions.)
  "recommendations": [
    "bierner.markdown-mermaid"
  ]
}
